## Yolo Test Model For Classification

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![YOLO](https://img.shields.io/badge/Ultralytics-YOLO-purple)

The program use for testing yolo model classification including a simple menu terminal to load or download various version of yolo, and search some image to be classified from yolo model.

## 🛠️ Prerequisites

Before running the program, ensure you have the following installed:
* **Python 3.8+**

## 📦 Installation

1.  **Clone the repository** (if you are using Git):
    ```bash
    git clone (https://github.com/marinternet/hvac-pipe-measure.git)
    cd hvac-pipe-measure/Yolo_Test
    ```

    Note: if you already cloned, then ignore the git clone command

2.  **Install Dependencies**:
    Use the provided `requirements.txt` file to install the necessary libraries.
    ```bash
    pip install -r requirements.txt
    ```

## 🚀 Usage

To start the detection test program, run the Python script:

```bash
python Test_Model.py
```
